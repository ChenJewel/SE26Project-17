# 下一步执行清单

更新时间：2026-07-15

## 当前状态

已完成：

- [x] 确认本地开发电脑是 Windows。
- [x] 确认云主机系统为 Ubuntu 22.04.5 LTS。
- [x] 确认云主机浮动 IP：`10.119.5.83`。
- [x] 确认最终展示路线：React/Vite Web -> Capacitor Android -> APK。
- [x] 新增 `server/` TypeScript + Express 后端。
- [x] 云端部署 Node 后端，systemd 服务名为 `ueat-server`。
- [x] 云端部署网页，访问地址：`http://10.119.5.83/`。
- [x] 云端部署 API，访问地址：`http://10.119.5.83/api`。
- [x] Nginx 已配置 `/` 托管网页、`/api/` 反向代理 Node。
- [x] 云端 PostgreSQL 数据库：`postgresql:///ueat?host=/var/run/postgresql`。
- [x] Auth 基础接口已接 PostgreSQL。
- [x] 约饭卡主链路已接 PostgreSQL。
- [x] 前端 `useMealCards` 已接 `GET/POST /meal-cards`，并保留本地 fallback。
- [x] 社区帖子、评论、点赞、收藏、转发主链路已接 PostgreSQL。
- [x] 评论编辑/删除已补充作者/admin 权限校验。
- [x] 关注、拉黑、举报、通知、个人主页资料已接 PostgreSQL。
- [x] 聊天会话、私聊、群聊、群聊广场、消息、撤回、已读、正在输入已接 PostgreSQL 和 WebSocket。
- [x] 帖子多图/视频 URL、聊天图片/语音消息已接本地 uploads 原型。

当前仍未完成：

- [ ] JWT 或 Cookie session 替换临时 `x-user-id`。
- [ ] 媒体上传从本地 uploads 原型迁移到对象存储或签名直传。
- [ ] 管理后台和举报审核页面。
- [ ] PostgreSQL 自动备份与恢复演练。
- [ ] 端到端自动化测试。
- [ ] Android APK 打包联调。
- [ ] 演示前备份和检查。

## 当前验证地址

```text
网页：http://10.119.5.83/
健康检查：http://10.119.5.83/api/health
约饭卡：http://10.119.5.83/api/meal-cards
```

云主机检查命令：

```bash
systemctl status ueat-server --no-pager
curl http://127.0.0.1:3000/health
curl http://127.0.0.1/api/health
sudo -u postgres psql -d ueat -c "\dt"
```

本地部署命令：

```powershell
cd "D:\U eat"
powershell -ExecutionPolicy Bypass -File deploy-cloud.ps1
```

## 推荐继续顺序

### 1. 认证安全和生产化

给 Codex 使用：

```text
请读取 TechPrototype/Codex任务提示词-App与云后端.md 的“后续任务提示词：认证安全和生产化”，把临时 x-user-id 替换为 JWT 或 Cookie session，并升级密码哈希。
```

### 2. 媒体上传生产化

给 Codex 使用：

```text
请读取 TechPrototype/Codex任务提示词-App与云后端.md 的“后续任务提示词：媒体上传”，把头像、帖子图片/视频、聊天图片/语音从云主机本地 uploads 原型迁移到对象存储或签名直传。
```

### 3. 管理后台和举报审核

给 Codex 使用：

```text
请补充 reports 管理后台页面，支持查看举报、审核通过/驳回，并保留管理员权限校验。
```

### 4. Android APK 打包联调

给 Codex 使用：

```text
请读取 TechPrototype/Codex任务提示词-App与云后端.md 的“后续任务提示词：Android APK 打包联调”，使用 http://10.119.5.83/api 作为生产 API 地址，完成 Capacitor Android 同步和 APK 打包验证。
```

### 5. 上线检查和备份

给 Codex 使用：

```text
请读取 TechPrototype/Codex任务提示词-App与云后端.md 的“后续任务提示词：上线检查和备份”，检查云端网页、API、PostgreSQL、Nginx、systemd 日志，并输出演示当天故障排查清单。
```

## 当前不要做

- 不要重写成 Taro。
- 不要做微信小程序。
- 不要重建 Flutter/React Native。
- 不要让 App 请求 `localhost`。
- 不要直接开放所有端口。
- 不要把密码写进 Git。
- 不要在页面组件里散落 fetch。
