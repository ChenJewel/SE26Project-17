# 下一步执行清单

更新时间：2026-07-14

## 当前状态

已完成：

- [x] 确认本地开发电脑是 Windows。
- [x] 确认云主机系统为 Ubuntu 22.04.5 LTS。
- [x] 确认云主机浮动 IP：`10.119.5.83`。
- [x] 确认最终展示路线：React/Vite Web -> Capacitor Android -> APK。
- [x] 新增 `server/` TypeScript + Express 后端。
- [x] 云端部署 Node 后端，systemd 服务名为 `ueat-server`。
- [x] 云端部署网页，访问地址：`http://10.119.5.83/`。
- [x] 云端部署 API，访问地址：`http://10.119.5.83/api`。
- [x] Nginx 已配置 `/` 托管网页、`/api/` 反向代理 Node。
- [x] 云端 SQLite 数据库：`/opt/ueat/server/data/ueat-dev.sqlite`。
- [x] Auth 基础接口已接 SQLite。
- [x] 约饭卡主链路已接 SQLite。
- [x] 前端 `useMealCards` 已接 `GET/POST /meal-cards`，并保留本地 fallback。

当前仍未完成：

- [ ] 社区帖子和评论入库。
- [ ] 点赞、收藏、关注、拉黑、举报入库。
- [ ] 聊天会话和消息入库。
- [ ] WebSocket 实时聊天。
- [ ] 通知入库和前端接入。
- [ ] 媒体上传。
- [ ] 正式认证安全升级，替换临时 `x-user-id`。
- [ ] Android APK 打包联调。
- [ ] 演示前备份和检查。

## 当前验证地址

```text
网页：http://10.119.5.83/
健康检查：http://10.119.5.83/api/health
约饭卡：http://10.119.5.83/api/meal-cards
```

云主机检查命令：

```bash
systemctl status ueat-server --no-pager
curl http://127.0.0.1:3000/health
curl http://127.0.0.1/api/health
ls -lh /opt/ueat/server/data/ueat-dev.sqlite
```

本地部署命令：

```powershell
cd "D:\U eat"
powershell -ExecutionPolicy Bypass -File deploy-cloud.ps1
```

## 推荐继续顺序

### 1. 社区帖子和评论入库

给 Codex 使用：

```text
请读取 TechPrototype/Codex任务提示词-App与云后端.md 的“后续任务提示词：社区帖子和评论入库”，继续完成社区帖子和评论的 SQLite 数据库接入，并替换 web/src/hooks/useCommunityState.ts。完成后部署到云主机并验证 http://10.119.5.83/api/posts。
```

### 2. 点赞、收藏、关注、拉黑、举报入库

给 Codex 使用：

```text
请读取 TechPrototype/Codex任务提示词-App与云后端.md 的“后续任务提示词：点赞、收藏、关注、拉黑、举报入库”，继续完成互动关系数据库化，前端替换本地互动状态，完成后部署到云主机验证。
```

### 3. 聊天消息入库

给 Codex 使用：

```text
请读取 TechPrototype/Codex任务提示词-App与云后端.md 的“后续任务提示词：聊天消息入库”，把 conversations、messages 和 meal exchange request 接入 SQLite，并让前端 Chat 页从 API 加载。
```

### 4. 实时聊天 WebSocket

给 Codex 使用：

```text
请读取 TechPrototype/Codex任务提示词-App与云后端.md 的“后续任务提示词：实时聊天 WebSocket”，在消息入库基础上增加 Socket.IO 或 WebSocket 实时聊天，并通过 Nginx /socket.io/ 代理验证。
```

### 5. 通知入库和前端接入

给 Codex 使用：

```text
请读取 TechPrototype/Codex任务提示词-App与云后端.md 的“后续任务提示词：通知入库和前端接入”，实现 notifications 表、通知 API 和 NotificationPanel 接入。
```

### 6. 媒体上传

给 Codex 使用：

```text
请读取 TechPrototype/Codex任务提示词-App与云后端.md 的“后续任务提示词：媒体上传”，实现头像和帖子图片上传，先使用云主机本地 uploads 目录，后续再迁移对象存储。
```

### 7. 认证安全和生产化

给 Codex 使用：

```text
请读取 TechPrototype/Codex任务提示词-App与云后端.md 的“后续任务提示词：认证安全和生产化”，把临时 x-user-id 替换为 JWT 或 Cookie session，并升级密码哈希。
```

### 8. Android APK 打包联调

给 Codex 使用：

```text
请读取 TechPrototype/Codex任务提示词-App与云后端.md 的“后续任务提示词：Android APK 打包联调”，使用 http://10.119.5.83/api 作为生产 API 地址，完成 Capacitor Android 同步和 APK 打包验证。
```

### 9. 上线检查和备份

给 Codex 使用：

```text
请读取 TechPrototype/Codex任务提示词-App与云后端.md 的“后续任务提示词：上线检查和备份”，检查云端网页、API、SQLite、Nginx、systemd 日志，并输出演示当天故障排查清单。
```

## 当前不要做

- 不要重写成 Taro。
- 不要做微信小程序。
- 不要重建 Flutter/React Native。
- 不要让 App 请求 `localhost`。
- 不要直接开放所有端口。
- 不要把密码写进 Git。
- 不要在页面组件里散落 fetch。
